A simple chat app to send and recieve messages.

For client install:
"*DragonChat*" directory

For server install:
"*DragonChatServer*" directory

**Initialize the variable *link* in the file "*DragonChat/index.js*" to
the server's URL.**
